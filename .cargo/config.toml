# llvm-sys を libLLVM-19.so への動的リンクに固定する。
# Debian の llvm-19 は shared モード (libPolly.a 等の静的コンポーネントを同梱しない)
# ため、デフォルトの静的リンクは "could not find native static library `Polly`" で失敗する。
# llvm-config も --shared-mode = shared を報告する。
[env]
LLVM_SYS_191_PREFER_DYNAMIC = "1"
