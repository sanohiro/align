[package]
name = "align_runtime"
version.workspace = true
edition.workspace = true
license.workspace = true

# 生成コードにリンクする薄いネイティブランタイム (docs/impl/06-runtime-std.md)。
# M0 では ABI のスケッチのみ。codegen 結線時に staticlib として出力する。
