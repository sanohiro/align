"""Trusted evidence-controller building blocks.

The package deliberately depends only on the Python 3 standard library.  The
installed launcher and verifier will import these modules only after checking
their root-owned installation manifest.
"""
