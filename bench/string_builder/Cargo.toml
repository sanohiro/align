[package]
name = "string-builder-bench"
version = "0.0.0"
edition = "2021"
publish = false

# Detached from the parent Align workspace.
[workspace]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
